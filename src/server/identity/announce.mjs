/**
 * Cardinal Frame — Identity Announce — First-Person News
 *
 * When the identity changes — new face, new voice, new name — the companion
 * announces it in first person. "I have a new look" lands differently from
 * a system notification; the companion owns its own changes the way a
 * person would.
 *
 * v2 scaffold — contract surface only, no logic yet.
 * Dependencies (planned): identity.mjs
 */


const SCAFFOLD = 'v2 scaffold — not implemented yet';

function notImplemented(fn) {
  throw new Error(`[v2-scaffold] ${{fn}}: ${{SCAFFOLD}}`);
}

export async function announce(change) {
  notImplemented('announce');
}
