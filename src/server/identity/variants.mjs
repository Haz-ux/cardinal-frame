/**
 * Cardinal Frame — Avatar Variants — Edits of the Master
 *
 * Expression, seasonal, and holographic-map-style variants are edits OF the
 * master, never fresh generations. Each variant carries its master lineage
 * so the face stays locked across every context it appears in.
 *
 * v2 scaffold — contract surface only, no logic yet.
 * Dependencies (planned): identity.mjs, avatar-create.mjs
 */


const SCAFFOLD = 'v2 scaffold — not implemented yet';

function notImplemented(fn) {
  throw new Error(`[v2-scaffold] ${fn}: ${SCAFFOLD}`);
}

export async function createVariant(masterId, kind) {
  notImplemented('createVariant');
}
export async function listVariants(masterId) {
  notImplemented('listVariants');
}
