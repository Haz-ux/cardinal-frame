/**
 * Cardinal Frame v2 scaffold — PresenceFeed
 *
 * Future: the proactive feed — presence.mjs surfacings rendered as they
 * happen, before they become notifications. The pulse stream of the machine
 * noticing things.
 */
import React from 'react';

export default function PresenceFeed() {
  return (
    <div className="presencefeed" data-scaffold="v2">
      <p>PresenceFeed — v2 scaffold. The real surface will live here.</p>
    </div>
  );
}
