/**
 * Cardinal Frame v2 scaffold — IdentityPanel
 *
 * Future: identity editing — name, character anchor, vibe, color language,
 * style anchors, and voice casting per persona. Writes to the singleton
 * identity record; avatar/voice changes go through staging + activation.
 */
import React from 'react';

export default function IdentityPanel() {
  return (
    <div className="identitypanel" data-scaffold="v2">
      <p>IdentityPanel — v2 scaffold. The real surface will live here.</p>
    </div>
  );
}
