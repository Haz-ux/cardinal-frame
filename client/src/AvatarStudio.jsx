/**
 * Cardinal Frame v2 scaffold — AvatarStudio
 *
 * Future: avatar candidate staging and activation — review staged candidates,
 * pick one explicitly, browse history, restore. The consent gate for the
 * companion face, backed by routes/identity.mjs.
 */
import React from 'react';

export default function AvatarStudio() {
  return (
    <div className="avatarstudio" data-scaffold="v2">
      <p>AvatarStudio — v2 scaffold. The real surface will live here.</p>
    </div>
  );
}
